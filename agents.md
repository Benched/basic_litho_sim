The project needs to build to be usable with GitHub pages.

The html pages have to work in browser without programming environments being a prerequisite.

Testing  is compatible with pytest.

Commits are only allowed after pytest succeeds.

Concepts that are tested for need to stay tested for, unless the change in production code is deliberately breaking the premise of the test.

git commits by ai agents need to start with the name of the agent

